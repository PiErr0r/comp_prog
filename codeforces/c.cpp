#include <bits/stdc++.h>

using namespace std;

void solve(void) {
  int n, m;
  cin >> n >> m;
  vector<int> v(n);
  for (int &i : v) cin >> i;

}

int main(void) {

  int T, i;
  cin >> T;

  for (i = 1; i <= T; ++i) {
    // cout << "Case #" << (i) << ": ";
    solve();
    cout << endl;
  }

  return 0;
}
